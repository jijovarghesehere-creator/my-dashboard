{
  "name": "my-dashboard",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:cf": "npm run build && wrangler pages dev dist"
  },
  "devDependencies": {
    "vite": "^8.0.12",
    "wrangler": "^4.24.0"
  }
}
